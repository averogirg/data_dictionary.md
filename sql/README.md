## 🔍 SQL-анализ: отвечаю на вопросы бизнеса

Я изучала SQL на практике, ставя перед собой конкретные вопросы, на которые можно ответить с помощью данных. Вот ключевые из них:

**1. С чего начать? Знакомство с данными.**  

SELECT * FROM superstore LIMIT 5;

**2. Какие товары самые дорогие? Анализ ассортимента.**

SELECT product_name, sales FROM superstore ORDER BY sales DESC;

**3. Какая категория самая важная для бизнеса? Поиск ключевого драйвера выручки.**

SELECT category,
    SUM(sales) as total_sales,
    COUNT(*) as orders_count
FROM superstore
GROUP BY category
ORDER BY total_sales DESC;

**4. Кто наши главные клиенты? Выявление целевой аудитории.**

SELECT customer_name,
    SUM(sales) as total_spent,
    COUNT(*) as orders_count
FROM superstore
GROUP BY customer_name
ORDER BY total_spent DESC
LIMIT 5;

Все SQL-запросы доступны в папке /sql проекта.
